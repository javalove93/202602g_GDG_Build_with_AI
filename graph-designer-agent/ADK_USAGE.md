# Graph Designer ADK Agent

이 디렉토리는 ADK Agent 프로젝트의 루트입니다.

## ADK 실행 방법

```bash
# 가상환경 활성화
source .venv/bin/activate

# ADK 웹 서버 시작 (루트 디렉토리에서)
cd ..
adk web graph-designer-agent
```

이렇게 하면 `graph-designer-agent/agent.yaml`이 Main Agent로 로드됩니다.
